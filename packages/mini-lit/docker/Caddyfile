:80 {
	root * /srv

	# Enable compression for all responses
	encode {
		zstd
		gzip
		minimum_length 256
	}

	# Serve files with proper fallback
	handle {
		try_files {path} {path}.html /index.html
		file_server
	}
}