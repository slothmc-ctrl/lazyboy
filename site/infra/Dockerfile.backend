FROM node:22-bookworm-slim

WORKDIR /app

# Start script - use exec to ensure Node receives signals directly
CMD ["sh", "-c", "npm ci --omit=dev --ignore-scripts && exec node dist/backend/server.js"]
